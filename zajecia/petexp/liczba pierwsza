#include <iostream>
#include <string>
#include <math.h>
#include <stdio.h>
using namespace std;
int main(){
int a;
int c=0;
cin>>a;
float b=pow(a,0.5);
for(int i=2;i<=b;i++){
    if (a%i == 0){
    cout<<"zlozona"<<endl;
    c=1;
    break;
}
}
if (c == 0)
cout<<"pierwsza";
}
